# claunch — @clack/prompts prototype

TUI preset picker for Claude Code flags. Select a saved preset and get a ready-to-run `claude` command.

## Run

```bash
npm install
npm start
```

## Non-interactive fallback (default preset, no menu)

```bash
npm start -- --non-interactive
```

Expected output: `$ claude --model sonnet`
